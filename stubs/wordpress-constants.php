<?php

/**
 * Development-only WordPress constants for static analysis.
 *
 * This file is not loaded by the plugin at runtime.
 */

defined( 'ABSPATH' ) || define( 'ABSPATH', '' );
defined( 'WP_CONTENT_DIR' ) || define( 'WP_CONTENT_DIR', '' );
defined( 'HOUR_IN_SECONDS' ) || define( 'HOUR_IN_SECONDS', 3600 );
defined( 'DAY_IN_SECONDS' ) || define( 'DAY_IN_SECONDS', 86400 );
defined( 'WP_PLUGIN_DIR' ) || define( 'WP_PLUGIN_DIR', '' );