# Rank Math Modified Date Lock

A WordPress plugin to lock the modified date of posts and pages when using Rank Math SEO.

## Development

This repository contains the source code for the `brs-rank-math-modified-date-lock` WordPress plugin.

Development dependencies are installed with Composer:

```powershell
composer install
```

The `vendor` directory is development-only and is not committed or deployed.
